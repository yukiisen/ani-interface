import { ColorableDirective } from './colorable.directive';

describe('ColorableDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorableDirective();
    expect(directive).toBeTruthy();
  });
});
